      - name: Check APY & Send Telegram
        run: |
          echo "📡 Recupero APY da Beefy..."

          RAW=$(curl -s https://api.beefy.finance/apy)
          echo "📦 JSON: $RAW" | head -c 300

          APY=$(echo "$RAW" | jq -r '."stargate-v2-arb-usdt"')
          echo "🧪 DEBUG APY grezzo = $APY"

          # Calcolo percentuale corretta
          APY_INT=$(awk "BEGIN {printf \"%.2f\", $APY * 100}")
          echo "✅ APY in % = $APY_INT"

          MSG="Rendimento attuale del vault Beefy: ${APY_INT}%"

          curl -s "https://api.telegram.org/bot${{ secrets.TELEGRAM_TOKEN }}/sendMessage" \
            -d chat_id=${{ secrets.CHAT_ID }} \
            -d text="$MSG"
